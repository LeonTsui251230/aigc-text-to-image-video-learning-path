# 图生图与局部重绘

## 本章定位与学习目标

前几章你从「文字」出发生成图。本章进入「以图为起点」：用一张已有图做底，让模型在其基础上改造（图生图），或只重画图的某一部分（局部重绘）。学完本章，你能把「差一点的图」改成「正好」，能做角色换装、背景替换、瑕疵修补，控制力再上一档。

图生图/局部重绘是「精修阶段」的核心工具，也是连接文生图与后期制作的桥梁。它把生成从「一次性抽奖」变成「可雕刻的过程」。

---

## 一、核心概念：从「无中生有」到「有中改之」

文生图是「给文字，出图」；图生图是「给一张图 + 文字，出一张改造后的图」。区别在于：模型不再从纯噪声起步，而是从你的图出发，按提示做「有限改造」。

这带来两个好处：
1. **保结构**：主体姿态、构图被保留，只改你想要的。
2. **可控**：你给的图本身就约束了结果，比纯文字更准。

> 关键认知：**图生图 = 在已有图附近的潜空间里再做去噪**。改造幅度由「重绘幅度」控制。

---

## 二、重绘幅度（Denoise Strength）：图生图最关键的旋钮

重绘幅度决定「在底图上改多少」：

| 幅度 | 表现 | 适用 |
| --- | --- | --- |
| 低（0.1–0.3） | 几乎原图，仅微调色彩/质感 | 轻微风格化、调色 |
| 中（0.4–0.6） | 保留构图，明显改风格/元素 | 换风格、换衣、换背景 |
| 高（0.7–1.0） | 大幅重画，仅留大致轮廓 | 彻底改造、构图重来 |

直觉：幅度低=「在原图上化妆」，幅度高=「照原图轮廓重画一幅」。

调优法：想保姿态只改风格 → 0.3–0.5；想换场景但留主体 → 0.5–0.7；只想微调 → 0.2。幅度过高会「认不出原图」，过低则「改不动」。

---

## 三、图生图典型用法一：风格迁移

把一张写实照变成插画，或把插画变水彩。做法：

1. 上传原图（如一张人物照）。
2. 提示写目标风格：「转为治愈系插画，柔和笔触」。
3. 重绘幅度设 0.5–0.6（保留人脸结构，换画风）。
4. 生成，挑自然的一张。

注意：幅度过高人脸会「变一个人」，所以人像风格化要偏低幅度（0.3–0.45）并配合面部相关负面词。

---

## 四、图生图典型用法二：换元素/换装

保留主体与构图，改局部元素：

- 换衣服：原图人穿红裙 → 提示「改为蓝色长袍」，幅度 0.5。
- 换季节：夏景 → 提示「转为冬日雪景，保留构图」，幅度 0.6。
- 换时段：白天 → 「黄昏暖调」，幅度 0.4。

要点：提示要写「保留什么 + 改什么」，模型才知道边界。只写「改蓝袍」可能连背景一起蓝，提示补「背景保持不变」更稳。

---

## 五、图生图典型用法三：提升与扩展

- **放大重绘**：小图放大时顺带用图生图补细节（幅度 0.2–0.35），比单纯拉伸清晰。
- **外扩（扩图）**：原图是方，想变横，用「外扩」让模型补出左右内容，保持主体居中。扩图幅度可略高（0.5–0.7）以生成协调的新区域。
- **补全**：图边缘被裁，扩图补回环境。

扩图是做不同比例适配的神器：一张竖图扩成横版封面，主体不动，两侧补场景。

---

## 六、局部重绘（Inpainting）：只改一块

局部重绘是图生图的「精准版」：你用蒙版圈出要改的区域，模型只重画那块，其余原样保留。

流程：
1. 上传图，用画笔在想改处涂蒙版。
2. 写该区域的提示（只描述这块要什么）。
3. 设该区域重绘幅度（通常 0.5–0.8，因只改局部可稍高）。
4. 生成，蒙版外完全不变。

典型场景：
- 修手指：圈出手部，提示「正常的五指手」，幅度 0.7。
- 换背景：圈出背景，提示「星空城市」，幅度 0.7。
- 去水印/杂物：圈出，提示「干净背景」，幅度 0.6。

---

## 七、蒙版技巧：圈得准，改得稳

蒙版质量直接决定局部重绘成败：

- **圈大一点**：蒙版比目标略大，留过渡带，避免硬边。
- **羽化/柔边**：软边让重绘区与原图自然融合，硬边易现接缝。
- **只描述区域内**：局部提示别写全局，否则模型可能改到蒙版外（虽通常受限）。
- **多试几次**：同一蒙版不同种子，挑融合最好的一张。

---

## 八、局部重绘的「内容感知」填充

有些平台支持「不写提示，只圈区域，让模型自动补全合理内容」（类似智能填充）。适合：

- 去掉图中某个物体，让背景自然接上。
- 扩大留白区，自动补环境。

此法快但不完全可控，重要修改仍建议带提示。

---

## 九、图生图示例一：照片转插画

在主流文生图平台操作：

1. 上传一张人物照片作为底图。
2. 正向提示：
```
转为温暖治愈系插画，柔和笔触，保留人物特征与构图
```
3. 负面提示：畸变，变形，多余手指，模糊。
4. 重绘幅度：0.4。
5. 步数 30，CFG 9。
6. 生成，挑人脸自然的一张；若脸变样，幅度降到 0.3 重试。

---

## 十、图生图示例二：换季节背景

在主流文生图平台操作：

1. 上传一张夏日庭院图。
2. 正向提示：
```
保留主体构图，将场景转为深秋，金黄落叶，暖阳，其余不变
```
3. 重绘幅度：0.55。
4. 生成，检查主体（如人物/建筑）是否保持、仅环境变秋。
5. 若主体也被改，幅度降到 0.45 并加「主体不变」。

---

## 十一、局部重绘示例：修一只「多指手」

在主流文生图平台操作：

1. 上传有瑕疵的图，用画笔圈出手部（略大于手）。
2. 区域提示：`一只手，五指正常，自然姿态，皮肤质感`。
3. 负面：`多余手指，畸形手，畸变`。
4. 区域重绘幅度：0.7。
5. 生成，挑手指正常、与手臂衔接自然的一张。
6. 若硬边明显，扩大蒙版或开启柔边重试。

---

## 十二、参数说明表（图生图相关）

| 参数 | 含义 | 建议 | 调高影响 |
| --- | --- | --- | --- |
| 重绘幅度 | 改底图多少 | 0.2–0.7 | 越界越大，过高失原图 |
| 步数 | 去噪次数 | 25–35 | 更细 |
| CFG | 提示服从 | 7–11 | 更贴提示 |
| 蒙版柔边 | 融合软硬 | 开 | 更自然融合 |
| 种子 | 复现 | 任意 | 固定可重复 |

局部重绘时，幅度可高于整图（因只改局部），但过高仍会「画出界」。

---

## 十三、常见错误与避坑清单

1. **幅度设太高**：原图全变，白传底图。想保结构就低幅度。
2. **图生图只写风格不写「保留」**：模型可能连主体一起改。提示补「保留构图/主体」。
3. **局部提示写全局**：圈手却写「全身换装」，干扰。
4. **蒙版太紧**：硬边接缝明显，应略大+柔边。
5. **用图生图生成精确文字**：仍不行，文字需后期。
6. **扩图不留主体空间**：扩横图时主体被挤边，应先留位或后期移。

---

## 十四、进阶技巧

- **垫图控构**：图生图最稳的用法是「用草图垫底，出精细图」——先画简陋构图，幅度 0.6 出成品。
- **局部+整体结合**：先整图换风格（低幅），再局部修瑕疵（高幅），分层处理。
- **种子继承**：图生图固定种子，小幅改提示做变体，风格连贯。
- **蒙版分层**：大改分多次（先背景后主体），每次幅度适中，比一次高幅更可控。
- **扩图做多比例**：一张方图扩成横/竖各一，适配不同渠道。

---

## 十五、实战小案例：把草图变成成品插画

目标：你手绘了一张简陋的「猫坐窗台」线稿，想出成精致插画。

步骤：
1. 拍/扫草图，上传为底图。
2. 正向提示：
```
将线稿转为精致治愈插画：橘猫坐窗台，午后阳光，绿植，柔和色彩
```
3. 负面：畸变，模糊，多余手指，乱码文字。
4. 重绘幅度：0.6（保留构图，充分细化）。
5. 步数 30，CFG 9，生成 4 张。
6. 挑最贴近草图意图的一张；若某处（如脸）不满意，局部重绘修。
7. 定稿后可再扩图加留白做封面。

检验：成品是否「认得出是同一构图」，且细节明显提升。

---

## 十六、实战小案例二：电商图换背景

目标：产品图原是白底，想换成「浅木台面 + 暖光」高级场景。

步骤：
1. 上传白底产品图。
2. 正向提示：`保留产品本体与比例，替换为浅木台面场景，左侧柔光，干净高级感`。
3. 重绘幅度：0.6（产品应低幅保形，但平台整图幅度统一，故用 0.5–0.6 并靠提示「保留产品」约束）。
4. 若产品被改，改用局部重绘：圈背景，提示换场景，幅度 0.7，产品区不动。
5. 生成，挑产品清晰、背景协调的一张。

检验：产品无变形、无反光异常，背景不抢主体。

---

## 十七、合规、版权与成本注意点

**合规**：
- 图生图的「底图」若含受限内容，改造后仍受限；不要用图生图绕过过滤。
- 用他人图片做底图改造，需确认你有权使用该图，否则涉侵权。

**版权**：
- 你生成的底图（如自绘草图）权属清晰；用网络图做底需授权。
- 改造后的成品，权属依平台协议与底图权属综合判断，商用前核查。

**成本**：
- 图生图/局部重绘仍需算力，幅度高、步数高更贵。
- 局部重绘只算局部，比整图重画省；多瑕疵可逐个局部修，避免整图重来。

---

## 十八、本章小结

本章你掌握了「以图为起点」的两大能力：
- 图生图：重绘幅度控改造量，用于风格迁移、换元素、放大扩展；
- 局部重绘：蒙版圈区、只改一块，用于修瑕疵、换背景、去杂物；
- 关键纪律：提示写「保留什么+改什么」，幅度按需，蒙版略大柔边。

到本章，文生图的「生成—调优—精修」闭环已完整。下一章（1-7）我们用「工作流」把所有能力串成可复用的生产流程。

---

## 十九、上手练习

1. 找一张自己的照片（或自绘草图），用图生图转成插画，重绘幅度先 0.4，再 0.6，对比保真度差异。
2. 用局部重绘修一张图中某个你不满意的局部（如换背景/修手），记录蒙版与幅度设置。
3. 取一张方图，用扩图变成横版，要求主体居中、两侧补协调场景，说明用途。

---

## 二十、图生图 vs 文生图：怎么选

一张表帮你决策：

| 你的情况 | 选文生图 | 选图生图 |
| --- | --- | --- |
| 脑中一张图，从零出 | ✅ |  |
| 已有草图/照片要精修 |  | ✅ |
| 想严格保构图只换风格 |  | ✅ |
| 想彻底重来 | ✅ | 幅度拉满也行 |
| 想修某处瑕疵 |  | 局部重绘 ✅ |
| 要做系列统一角色 | 固定种子 | 垫图控形更佳 |

经验：创作初期用文生图探索方向；方向定了用图生图锁定并精修；交付前用局部重绘修瑕疵。三段递进。

---

## 二十一、局部重绘的高阶玩法

除了「修瑕」，局部重绘还能做创意：

- **局部加细节**：圈出主体面部，提示「增加眼神光与皮肤质感」，幅度 0.5，让焦点更精。
- **局部换风格**：圈出背景，提示「转为油画笔触」，主体保持插画，制造混搭层次。
- **局部调光**：圈出某区，提示「补一盏暖光」，做局部打光。
- **多蒙版分批**：先圈背景换场景，再圈主体换衣，分步比一次高幅更可控。

核心：局部重绘让你「分图层思考」，像后期软件一样精准。

---

## 二十二、失败诊断表

| 现象 | 可能原因 | 解法 |
| --- | --- | --- |
| 图生图后认不出原图 | 幅度过高 | 降到 0.3–0.5 |
| 局部重绘出现硬边 | 蒙版太紧/无柔边 | 扩大蒙版+开柔边 |
| 换背景主体也变 | 提示没写「保留主体」 | 补「主体不变」+ 降幅 |
| 扩图后主体被裁 | 扩向不对/主体偏 | 先移主体或后期裁 |
| 修手后手与臂脱节 | 蒙版过小/幅度低 | 扩大蒙版+幅度 0.7 |
| 图生图变糊 | 步数低/幅度高噪 | 步数 30+，幅度适度 |

诊断时一次只调一项，对照此表定位最快。

---

## 二十三、常见疑问 FAQ

**Q1：图生图算「二创」还是「原图」？**
取决于改造幅度与底图权属。高幅度近似原创，低幅度偏改图；底图若非你所有需授权。

**Q2：局部重绘要开柔边吗？**
建议开。硬边易接缝，柔边让融合自然，尤其换背景时。

**Q3：扩图会损原图质量吗？**
新补区域是模型生成，可能略弱于原图；重要主体最好留在原图区不被重画。

**Q4：图生图能改构图吗？**
能但有限。小幅构图调整（平移主体）可用低幅+提示；大改建议重文生图或扩图。

**Q5：蒙版能多次叠加吗？**
可以，分批处理不同区域，每次幅度适中，比一次高幅稳。

---

## 二十四、图生图速查卡

- 重绘幅度：微调 0.2 / 换风 0.4–0.5 / 换景 0.55–0.7 / 大改 0.8+。
- 提示写「保留 X + 改 Y」，边界清晰。
- 局部重绘：蒙版略大+柔边，区域幅度可 0.5–0.8，只写区域内提示。
- 扩图：留主体空间，幅度 0.5–0.7 补新区域。
- 流程：文生图探方向 → 图生图锁形精修 → 局部重绘修瑕。
- 失败先降幅度、扩蒙版、补「保留」词。

把此卡与前几章卡片合订，即是「文生图全能手册」。

---

> 下一章「文生图工作流」：把提示词、参数、负面、图生图串成一套标准生产流程，让你稳定、批量、可复用地产出图。
