# 术语表

## AIGC

人工智能生成内容（AI Generated Content），指由模型自动生成文本、图像、音频、视频等内容的技术总称。

## 扩散模型（Diffusion Model）

一类生成模型，通过逐步去噪的方式从随机噪声中还原出图像。当前主流文生图系统的核心架构。

## 潜空间（Latent Space）

模型内部表示数据的压缩空间。在潜空间上做生成与编辑，比直接处理像素更高效。

## 提示词（Prompt）

用户输入给模型的文本指令，描述希望生成的内容。提示词质量直接决定输出质量。

## 负面提示词（Negative Prompt）

指定「不希望出现」的内容，例如变形、低清、多余手指，用于过滤劣质结果。

## 图生图（Image-to-Image）

以一张已有图像为参考，按提示词修改或重绘生成新图。

## 文生视频（Text-to-Video）

输入文本直接生成一段视频，模型需同时处理画面内容与时间维度。

## 图生视频（Image-to-Video）

以一张静态图为首帧，生成与之连贯的动态视频。

## 帧间一致性

视频相邻帧之间人物、场景、风格的连续程度。一致性差会表现为抖动、突变。

## 数字人

基于真人形象或虚拟形象生成的可驱动口播视频，常用于讲解、带货、课程。
