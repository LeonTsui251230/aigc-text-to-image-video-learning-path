# 术语表

本术语表解释 AIGC 文生图与视频领域常用概念。所有解释力求用大白话讲清直觉，不堆公式。

---

## A

**AIGC（人工智能生成内容）**
利用人工智能模型自动生成的文本、图像、音频、视频等内容。本仓库主要讨论其中的视觉生成部分。

**AI 漫剧**
使用 AI 生成图像或视频片段，再通过剪辑、配音、配乐组合成的短剧形式内容。

**Avatar（数字分身/虚拟形象）**
在视频或直播中代表用户的虚拟人物形象，常用于口播、客服、虚拟主播等场景。

---

## B

**步数（采样步数）**
模型从噪声逐步生成图像的迭代次数。步数越多，生成越精细，但耗时也越长。

**闭源模型**
不公开训练细节、代码和权重的模型，通常通过 API 或平台提供服务。

---

## C

**采样（Sampling）**
模型从潜在空间逐步采样生成图像或视频的过程。不同的采样策略会影响结果质量和风格。

**参考图**
用于图生图或图生视频的输入图片，模型会基于参考图的结构或风格生成新内容。

**参数调优**
调整生成过程中的各项设置（如步数、引导强度、分辨率等），以获得更理想的结果。

---

## D

**大模型**
参数量巨大、训练数据广泛的生成模型，能够理解复杂提示词并生成高质量内容。

**端到端生成**
从输入提示词直接输出最终图像或视频，无需中间人工干预的生成方式。

**多模态模型**
能同时理解和处理多种数据类型（如文本、图像、视频）的模型。

---

## E

**Embedding（嵌入）**
将文本、图像等内容转换成模型能理解的数值向量表示的过程。

---

## F

**负面提示词（Negative Prompt）**
告诉模型不要生成什么内容的描述。常用于排除畸形肢体、低质量、水印等元素。

**风格迁移**
将一张图片的内容与另一种风格结合，生成具有新风格的图像。

**分辨率**
生成图像或视频的像素尺寸，如 1024×1024、1920×1080 等。

---

## G

**GUI（图形用户界面）**
通过可视化界面操作生成平台，与命令行相对。多数主流平台提供 GUI。

---

## H

**画幅比例**
图像或视频的宽高比，如 1:1、16:9、9:16、2.35:1 等。不同平台/用途适合不同比例。

**后期处理**
生成后对图像或视频进行裁剪、调色、加字、去瑕疵等加工，使其可用。

---

## I

**推理（Inference）**
模型根据输入生成输出的过程。生成一张图或一段视频就是一次推理。

---

## J

**局部重绘**
只修改图像的某个局部区域，其他区域保持不变。常用于修复手部、换脸、改背景等。

**镜头语言**
通过景别、角度、运动等手法传递信息和情绪的视觉表达方式。

---

## K

**可控生成**
通过各种手段（提示词、参考图、参数、控制网等）让生成结果更稳定、更符合预期的技术方向。

---

## L

**拉远/推进（Zoom Out / Zoom In）**
镜头运动中远离或靠近主体的操作，也可指图像扩展或裁剪的功能。

**LoRA**
一种轻量级模型微调技术，可以用少量数据训练出特定风格或角色的生成能力。

---

## M

**模型上下文**
模型在一次生成中能够接收的提示词长度和参考信息量。超出上下文可能导致信息丢失。

**模糊（Blur）**
画面不清晰的现象，可能由提示词、参数、分辨率或模型能力导致。

---

## N

**拟真度**
生成内容看起来多么接近真实世界。拟真度高不代表质量好，要看是否符合创作目标。

---

## O

**开源模型**
公开代码、权重或训练方法的模型，用户可以在本地或自己的服务器上部署。

---

## P

**Prompt（提示词）**
用户输入给模型的文本指令，用于描述想要生成的内容。是 AIGC 最核心的交互方式。

**Prompt Engineering（提示词工程）**
研究如何写出高质量提示词的技术和方法论。

---

## Q

**潜空间（Latent Space）**
模型内部的一种压缩表示空间。图像和视频不是直接在像素空间生成，而是先在潜空间运算，再解码成可见内容。

---

## R

**RAG（检索增强生成）**
虽然常用于文本领域，但在视觉领域也指通过检索参考图、风格库来增强生成质量的方法。

**分辨率升级（Upscale）**
将生成的小图通过算法放大到更高分辨率，同时尽量保持清晰度。

---

## S

**随机种子（Seed）**
控制生成随机性的数值。固定种子可以在相同条件下复现相似结果。

**采样器（Sampler）**
决定模型如何从噪声中采样生成图像的算法。不同采样器影响生成速度和风格。

**深度伪造（Deepfake）**
利用 AI 替换视频中人物面部或声音的技术，涉及较高法律和伦理风险。

---

## T

**图生图（Image-to-Image）**
以一张图片为输入，根据提示词生成新图片。常用于风格迁移、局部修改。

**图生视频（Image-to-Video）**
以一张图片为首帧，根据提示词生成后续动态画面。一致性通常优于文生视频。

**提示词权重**
控制提示词中某部分重要程度的方法。加权重会让模型更重视该描述。

---

## U

**Upscaling**
见「分辨率升级」。

---

## V

**VAE（变分自编码器）**
负责在潜空间和像素空间之间转换的模型组件。不同 VAE 会影响画面色彩和清晰度。

**视频延展**
在已有视频片段基础上继续生成后续内容，用于延长视频时长。

---

## W

**文生图（Text-to-Image）**
根据文本提示词生成静态图像。

**文生视频（Text-to-Video）**
根据文本提示词生成动态视频。

**Workflow（工作流）**
从需求到最终成片的完整生产流程，包括提示词、生成、后期、交付等环节。

---

## X

**像素空间**
图像或视频最终呈现的实际像素表示空间，与「潜空间」相对。

**显存**
显卡用于临时存储数据的内存。视频生成和大图生成对显存要求较高。

---

## Y

**样式参考**
作为风格锚点的参考图或提示词集合，用于保持多图/多片段风格一致。

**溢出（Out of Frame）**
主体部分超出画面边界。有时是构图需要，有时需要避免。

---

## Z

**帧率（Frame Rate）**
视频每秒播放的画面数量。常见有 24fps、30fps、60fps 等。

**帧间一致性**
视频相邻帧之间内容保持稳定、不跳变的能力。是衡量视频生成质量的重要指标。

**噪声（Noise）**
生成开始前模型接收的随机信号，是扩散模型生成图像的起点。

---

## 其他常用缩写

**API（应用程序接口）**
程序之间交互的接口。通过 API，你可以用代码调用生成平台的能力。

**MVP（最小可用产品）**
在 AIGC 学习中，指先用最少步骤做出一个能跑通的小作品。

**ROI（投资回报率）**
投入时间/金钱与产出价值之比。学习 AIGC 时建议优先做 ROI 高的场景。

---

> 本术语表会随着内容更新持续补充。如果你发现某个概念在正文中出现但这里没有解释，欢迎提 Issue。
