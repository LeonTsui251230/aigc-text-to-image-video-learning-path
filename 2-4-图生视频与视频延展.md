# 图生视频与视频延展

## 本章定位与学习目标

前两章你用纯文字生成视频，灵活但主体随机、一致性靠运气。这一章换思路：先有一张满意的图，再让它「动起来」——这就是图生视频。更进一步，你还能把一段短视频「续」长、把首尾帧钉死让中间自动补——这是视频延展。学完本章，你要掌握「用图锚定主体」的写法、理解首尾帧控制的威力，并学会把短片段拼成可用的长内容。它是 2-2、2-3 的自然延伸，也是后面数字人、一致性章节的基石。

---

## 一、核心概念：给一张图「注入时间」

把图生视频想成「给静止的照片按住播放键，并告诉它该怎么演」。你提供的那张图，就是视频的第一帧（首帧），模型在这个已知画面上生成后续帧，而不是从空白噪声里瞎猜主体。

这带来一个根本好处：**主体长什么样，由你的图说了算，不是模型随机抽**。这正是它比纯文生视频一致性更强的原因。

> 一句话：文生视频是「凭空编一段」，图生视频是「让你定好的主角接着演」。要主体可控，选后者。

---

## 二、为什么图生视频的一致性更好

对比两种路径：

| 维度 | 文生视频 | 图生视频 |
| --- | --- | --- |
| 主体外观 | 随机，每版不同 | 由首帧锁定 |
| 场景布局 | 随机 | 由首帧锁定 |
| 一致性 | 概率性，易翻车 | 锚定首帧，稳很多 |
| 灵活度 | 高，纯想象 | 受首帧限制 |
| 适合 | 试创意、氛围片 | 产品、人物、系列 |

本质上，图生视频把「最难的一致性难题」提前用一张图解决了——模型不用再「猜主体」，只需专注「在已知主体上怎么动」。任务变简单，成功率自然高。

---

## 三、首帧 / 参考帧的几种玩法

不同平台支持程度不同，但思路通用：

### 1. 仅首帧（最常见）

给第一帧，模型生成后续。主体、场景、风格全由首帧定，你只需写「怎么动 + 镜头」。

### 2. 首帧 + 尾帧（最强控制）

同时给第一帧和最后一帧，模型只生成中间过渡。你能精确控制「从 A 状态变到 B 状态」，比如「从合上的书变成翻开的书」。这是做确定性转场、产品变形演示的利器。

### 3. 参考帧（全程锚定）

部分能力支持在生成中多次参考某帧，防止中途主体漂移。适合长一点、要死守主体的片段。

> 用法直觉：**能传图就传图。** 哪怕只传首帧，一致性也远超纯文字。这是性价比最高的「稳片」手段。

---

## 四、图生视频的提示词怎么写

图生视频沿用 2-3 的六段式，但因为「主体 / 场景 / 风格」已被首帧锁定，提示词要「减负」：

| 段 | 文生视频里 | 图生视频里 |
| --- | --- | --- |
| 主体 | 详细写 | 可简写/不写（图已定） |
| 场景 | 详细写 | 可简写（图已定） |
| 动作 | 写 | 写（重点） |
| 镜头 | 写 | 写（重点） |
| 风格 | 写 | 可不写（图已定） |
| 质量 | 写 | 写（加稳定句） |

**关键点**：图生视频的提示词重心从「描述长什么样」转移到「描述怎么动、镜头怎么走」。主体段若写，应与首帧一致，别写矛盾（如首帧是猫却写狗，模型会纠结）。

---

## 五、视频延展：把短片段「续」长

纯生成通常只能出几秒。要更长，靠「视频延展」——拿已有片段的最后一帧当新首帧，接着生成后续，如此接力。

直觉：像接龙。第一段结尾的姿势，成为第二段开头的姿势，观感连续。

| 延展方式 | 做法 | 注意 |
| --- | --- | --- |
| 尾帧接首帧 | 上段末帧 → 下段首帧 | 姿势/场景要对得上 |
| 逐段延长 | 每次接上一段末尾 | 误差可能累积，定期回锚 |
| 指定尾帧收尾 | 最后一段给尾帧 | 控制结局，避免烂尾 |

风险：**越往后越容易偏**（误差累积）。应对：每隔几段用一张「校正图」当首帧拉回；或把长片拆成几个独立短镜，各自生成后剪辑拼接（更稳，见 2-6）。

---

## 六、首尾帧控制：精确动画的杀手锏

首尾帧（第一帧 + 最后一帧都指定）是做「确定性变化」的最强手段：

适用场景：
- 产品变形：瓶盖从合到开。
- 状态切换：白天变黑夜、枯树变花树。
- 转场：A 场景形变成 B 场景。
- 演示：图纸变实物。

写法：提示词重点写「中间如何过渡」，如「首帧闭合的花苞，尾帧盛开的玫瑰，中间花瓣自然展开」。主体两端都钉死，模型只补中间，结果高度可控。

> 这和纯文生视频的「随机演」完全不同：首尾帧是「你定起点和终点，模型走中间」。做教程、产品、演示类内容必学。

---

## 七、提示词示例（图生视频）

在主流视频生成平台使用以下提示词（先上传一张满意的首帧图）：

**示例 A：产品旋转（首帧锚定）**
```
（首帧：一张白底手机静物图）
主体：同首帧手机，外观锁定
场景：同首帧，白底简洁
动作：手机极缓慢原地自转约 40 度
镜头：固定俯拍，机位不动
质量：高细节，帧间稳定，无闪烁，边缘清晰
```

**示例 B：首尾帧状态切换**
```
（首帧：晴天的窗台；尾帧：同角度夜晚的窗台）
动作：天色由明转暗，灯光渐亮，窗帘微动
镜头：固定机位，不变
质量：风格一致，帧间平滑，无跳变
```

**示例 C：人物微表情（给数字人铺垫）**
```
（首帧：女生微笑正视镜头静图）
动作：保持微笑，轻微眨眼，头部极小幅度自然摆动
镜头：固定平视中景
质量：面部稳定，无变形，无闪烁
```

---

## 八、参数与模式说明

| 参数 | 图生视频里的意义 | 建议 |
| --- | --- | --- |
| 首帧/尾帧 | 锚定起点/终点 | 必传首帧，确定性需求传尾帧 |
| 运动幅度 | 在已知主体上怎么动 | 中低起步，确认稳再加 |
| 帧率 | 过渡顺滑度 | 24~30 |
| 时长 | 单段长度 | 3~5 秒，长片用延展接力 |
| 种子 | 复现同段 | 迭代固定 |
| 衔接模式 | 尾帧接下一段 | 延展时开启 |

注意：图生视频对「分辨率匹配」敏感——首帧分辨率和生成分辨率差太多，可能糊或裁切。尽量让首帧接近目标比例。

---

## 九、常见错误与避坑清单

1. **首帧太糊**：用低清图当首帧，视频全程糊。先超分（见 2-1）。
2. **提示词与首帧打架**：图是猫却写狗，模型纠结变形。保持一致的。
3. **尾帧和首帧角度差太大**：天地翻转，中间补不出。两端角度/构图接近。
4. **延展不接姿势**：上段结尾站、下段开头坐，跳变。衔接帧姿势要对上。
5. **运动幅度拉满**：已知主体上大幅动更易崩。中低起步。
6. **忘了稳定质量句**：图生视频也会闪。质量段照加「帧间稳定」。
7. **长片只靠延展不回锚**：越接越偏。定期用校正图拉回或拆镜拼接。
8. **首帧比例不对**：竖图强生成横片被裁。先裁好比例。

---

## 十、进阶技巧

### 1. 拆镜 + 各自图生视频 + 剪辑拼接

把长叙事拆成多个镜头，每个镜头用一张对应首帧图生视频，再剪辑拼。比「纯延展接力」稳得多，是商用长片主流做法（详见 2-6）。

### 2. 用尾帧做精准转场

A 镜尾帧 = B 镜首帧（同构图同主体），两段拼起来无缝。这是「匹配剪辑」的 AI 做法。

### 3. 循环素材：首尾相接

做烟、水、火、光晕等背景素材时，让尾帧接回首帧（或指定循环模式），得到可无缝循环的片段，做视频背景省心。

### 4. 校正图回锚法

长延展每接 2~3 段，就用一张「理想中间帧」当新首帧，把漂移拉回。像给长跑的人设补给站。

### 5. 首帧精修前置

首帧决定一切。生成首帧时就把构图、主体、光线调到最佳（用 1-4、1-6 的文生图技巧），视频质量事半功倍。

---

## 十一、实战小案例：一段「日出变正午」的衔接

**目标**：生成 4 秒「同一窗台从日出柔和光变为正午明亮光」的短片，要求场景不动、只光线变。

**做法**：
1. 先做两张图：首帧（日出暖光窗台）、尾帧（正午亮光同角度窗台），用文生图锁定同构图。
2. 上传为首帧、尾帧。
3. 提示词：动作写「天色由暖转亮，光照明度渐增，植物不动」；镜头固定；质量加「风格一致、帧间平滑」。
4. 参数：4 秒、24fps、低幅度（因只变光不变形）。

**验收**：场景纹丝不动，光线自然过渡，像延时摄影的一段。若场景元素跳变，说明首尾帧构图不一致，重做对齐。

---

## 十二、成本与合规注意点

- **首帧成本**：首帧图可能要多次文生图才满意，算进总成本。用 2-1 后期能降低废片。
- **延展算力**：长片靠多段生成，段数 × 单段成本。规划好拆分，别无脑续长。
- **素材版权**：首帧若用他人图片，图生视频属改编，需授权（详见 2-7）。
- **肖像**：用真人照片做首帧生成视频，涉肖像权与 likeness，勿商用。
- **输出音轨**：图生视频一般无声音，配乐另做并注意音乐版权。

---

## 十三（补一）、制作高质量首帧图的七条军规

首帧决定整段视频的天花板。结合前面文生图章节，给出专门针对图生视频的首帧要求：

1. **分辨率够高**：先超分（见 2-1），避免全程糊。
2. **比例对路**：按目标视频比例（9:16 / 16:9 / 1:1）先裁好，别让平台强裁。
3. **主体居中偏大**：给运动留余地，别顶边，防运动时出框。
4. **姿势「可动」**：选一个能自然接着动的姿势（如微侧身），别选极限动作（如腾空），模型难接。
5. **光线干净统一**：避免高速闪光、强逆光，这类光视频里最难稳。
6. **背景简洁**：杂乱背景视频里易闪，纯色或虚化最稳。
7. **风格锁定**：若做系列，首帧之间保持同一画风（用 2-1 统一调色）。

把这七条当首帧 checklist，能拦掉大部分「图生视频翻车」。

---

## 十三（补二）、视频延展的工程化工作流

把「续长」变成可管理的流水线，而不是无脑接力：

| 步骤 | 动作 | 目的 |
| --- | --- | --- |
| 1. 定总时长 | 拆成 N 个 3~5 秒段 | 控制单段稳定性 |
| 2. 定锚点 | 标出每段首/尾帧姿势 | 防累积漂移 |
| 3. 生成首段 | 首帧 + 动作 | 打底 |
| 4. 提取尾帧 | 取末帧当下段首帧 | 接力 |
| 5. 回锚检查 | 每 2~3 段用校正图 | 拉回漂移 |
| 6. 剪辑拼接 | 按脚本顺序接 | 成片 |
| 7. 统一调色 | 整片调色（见 2-1） | 风格一致 |

关键点：第 5 步「回锚」最易被省，但它是长片不崩的保险丝。哪怕多花一张校正图的成本，也值得。

---

## 十三（补三）、转场与匹配剪辑设计

AI 视频的「丝滑感」很大程度来自转场设计，而图生视频的首尾帧控制正好能做匹配剪辑：

- **同主体匹配**：A 镜尾帧的主体位置/大小 = B 镜首帧，拼接处看不出切。
- **动作接续**：A 镜人物抬手到一半，B 镜首帧接抬手后半，动作连贯。
- **状态渐变**：A 尾（黄昏）→ B 首（夜），用首尾帧过渡做时间流逝感。
- **形变异接**：A 尾（合上的书）→ B 首（翻开的书），中间用首尾帧补开合。

设计转场时，提前在分镜脚本里标好「哪两帧要对齐」，生成时就有明确目标，而不是生成完再硬剪。

---

## 十三（补四）、图生视频与剪辑软件的分工

图生视频负责「生成素材片段」，剪辑软件负责「把它们变成成片」。明确分工能极大提效：

| 环节 | 用图生视频 | 用剪辑软件 |
| --- | --- | --- |
| 生成动态片段 | ✅ | ❌ |
| 拼接多段 | ❌（易漂移） | ✅ |
| 加字幕/配音 | ❌ | ✅ |
| 调色统一 | 弱 | ✅（强） |
| 转场特效 | 弱（靠首尾帧） | ✅（硬切/叠化） |
| 节奏剪辑 | ❌ | ✅ |

心法：**AI 生成「食材」，剪辑软件「炒菜」**。别指望一段 AI 视频直接成片，过剪辑这道工序，专业感差一个层级。

---

## 十三（补五）、延展失败的诊断与修复

接力生成常出问题，给一份对照表，遇到症状直接查：

| 症状 | 根因 | 修复 |
| --- | --- | --- |
| 接驳处明显跳变 | 上段尾帧与下段首帧姿势/构图不一 | 重提尾帧当下段首帧，保证一致 |
| 越往后越偏色 | 误差累积 + 未回锚 | 插入校正图回锚 |
| 中段主体变形 | 单段太长 | 拆更短，每段 3 秒 |
| 光线突然变 | 首帧光影不稳 | 重做光影干净的首帧 |
| 背景元素闪 | 背景太杂 | 换简洁/虚化背景首帧 |
| 结尾烂尾 | 末段无尾帧约束 | 给最后一段指定尾帧收尾 |

记住：**绝大多数延展翻车，都能在「首帧质量」和「锚点对齐」两步找到原因**。先查这两处，别急着怪模型。

---

## 十四、本章小结与承上启下

- 图生视频 = 用首帧锚定主体，比纯文字一致性强得多。
- 提示词重心从「长什么样」转向「怎么动、镜头怎么走」。
- 首尾帧控制能做确定性动画/转场，是产品、教程类利器。
- 长片靠「拆镜各自生成 + 拼接」或「尾帧延展接力 + 定期回锚」，别无脑续。
- 首帧质量是天花板，先用文生图把首帧做精。

下一章（2-5）走进数字人与口播——本质上就是「用一张人像首帧 + 驱动动作/口型」的图生视频特例，本章的锚定思路直接复用。

---

## 十四（补）、能力边界与预期管理

图生视频虽稳，也有边界，提前管理预期：

- **首帧即天花板**：视频不会比首帧更精细，糊图生不出清视频。投资首帧质量最划算。
- **大幅变形仍难**：让静止主体做剧烈形变（如人变兽）易崩，适合用首尾帧做「可控渐变」而非「瞬间异变」。
- **复杂物理不支持**：碰撞、流体精确模拟仍弱，别指望用图生视频做精确特效。
- **时长有限**：单段几秒，长内容必须靠拆分/延展，没有「一键 1 分钟」的稳法。
- **音画分离**：生成的是无声画面，声音要另配。

理解边界，你才能把图生视频用在它擅长的地方（产品、人物、状态渐变、转场），而不是拿它硬刚不擅长的复杂动态。

---

## 十五、上手练习

1. 用文生图做一张满意的产品静物图（先超分），上传为首帧，在主流视频生成平台生成「缓慢旋转」3 秒片段，体会锚定带来的稳定性。
2. 做两张同构图的「白天/夜晚」图，用首尾帧生成「天色过渡」4 秒片段，检查场景是否纹丝不动。
3. 把一段 8 秒设想拆成「首帧 A → 尾帧 B → 首帧 B 接 C」的延展接力脚本，规划每段的首尾帧，理解长片如何靠锚点拼接。
