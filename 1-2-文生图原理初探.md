# 文生图原理初探

## 本章定位与学习目标

上一章你建立了「AIGC 是什么」的宏观直觉。本章我们往里走一层，搞清楚文生图模型内部到底发生了什么——不需要数学公式，但要把几个关键机制讲透：它怎么从一团噪声变出图、文字是怎么「指挥」画面的、为什么有「步数」和「引导强度」这些旋钮。

学完这一章，你调参数时不再是瞎蒙，而是知道每个旋钮在动什么。这会直接让你在后面写提示词、做参数调优时更有底气。

---

## 一、核心概念：扩散思想的大白话

当前主流文生图方法可以概括为一个词——**「逐步去噪」**。我们拆开看：

想象一张完全正常的照片，往上面不断撒「电视雪花噪点」，撒得越来越多，最后变成纯噪声，啥也看不出。这个过程叫「加噪」。模型在训练时，被教会做这件事的反面：给一张「噪声图 + 这是描述 X 的文字」，它预测「再去掉一点噪，图会更像 X」。

生成时，我们直接给一张纯噪声图 + 你的文字提示，让模型重复「预测并去掉一点噪」很多次。几十次之后，噪声里慢慢显出一只猫、一片海。这就是「扩散」二字的含义：信息从噪声中像墨在水里扩散那样，逐渐成形。

> 关键直觉：**生成不是「创造像素」，而是「从随机中恢复结构」**。模型每一步都在回答：「基于这段文字，当前的噪声里，哪些该留下、哪些该抹掉？」

---

## 二、去噪过程：画面是怎么一步步显形的

我们把一次生成拆成时间线，方便你建立画面感：

| 步数区间 | 画面状态 | 你能看到什么 |
| --- | --- | --- |
| 第 1–5 步 | 几乎纯噪 | 只有极淡的色块倾向（暖/冷） |
| 第 6–15 步 | 粗结构浮现 | 大致构图、主体轮廓、明暗分区 |
| 第 16–28 步 | 细节填充 | 毛发、纹理、五官、材质出现 |
| 第 29–40 步 | 收尾打磨 | 边缘锐化、噪点消除、整体协调 |

这就是为什么「步数」太低图会糊：结构还没定型就停了。但步数也不是越多越好——到一定步数后，画面基本不变，只是更慢更费。多数场景下 20–40 步是甜区。

实践含义：如果你只想快速看「构图对不对」，用 15–20 步小图先跑；确定构图后再用 30+ 步出成品。

---

## 三、潜空间：为什么不直接在像素上算

你可能会问：为什么不直接在原始像素上一步步去噪，非要弄个「潜空间」？

原因很简单——**效率**。一张图可能是百万级像素，在像素上每步运算都极其昂贵。模型先把图压缩到一个更小、更抽象的表示（潜空间），在那里做去噪，最后再「解压」回可见图像。这就像先画缩略草图定大局，再放大细化，而不是一上来就抠每个像素。

代价是：压缩会损失一点信息，所以极端细节（如清晰文字、精确小物件）容易失真。这也回扣了第一章说的「文字标志是弱项」。

---

## 四、文字如何变成「指挥棒」：文本编码

你的提示词不是被「读」的，而是被「编码」的。流程如下：

1. 提示词先被切成一个个片段（token），类似把句子拆成词块。
2. 一个文本编码器把这些片段变成一串数字向量，向量里装的是语义。
3. 这串向量在每一步去噪时，都「掺进」图像计算，告诉模型「往这个描述靠」。

于是同一个「猫」字，在「凶猛的猫」「可爱的猫」「卡通的猫」里，向量是不同的——因为上下文改变了语义。这解释了为什么**词的顺序和搭配很重要**：孤立堆词，不如用自然、连贯的描述。

进阶提示：风格词、镜头词（如特定画派名、镜头术语）在很多模型里有强对应向量，所以「加风格词效果立竿见影」。

---

## 五、引导强度（CFG）：听话与自由的旋钮

这是最该理解的一个参数。每一步去噪，模型有两个「声音」在拉扯：

- **声音 A（条件）**：听你的提示词，往描述靠。
- **声音 B（无条件）**：不顾提示，自由发挥它学到的「一般好看图」。

引导强度就是放大声音 A 的程度：

| 引导强度 | 表现 | 适用 |
| --- | --- | --- |
| 过低（<5） | 天马行空，但常跑题、主体不明确 | 抽象艺术、背景肌理 |
| 适中（7–12） | 贴合提示又自然，最常见区间 | 绝大多数场景 |
| 过高（>15） | 极度服从但易过饱和、锐化假、构图死板 | 需强约束时谨慎用 |

新手建议从 7–9 起步，觉得「没按我说的来」再略加；觉得「太死板」就略减。不要一上来拉到 20。

---

## 六、采样器：去噪的「算法口味」

「采样器」决定每一步去噪具体怎么算。不同采样器像不同画师的笔法：

- 有的快但糙，少步数就能出形；
- 有的慢但稳，多步数更干净；
- 有的擅长写实，有的擅长插画感。

对使用者来说，**不必深究每种算法的数学差异**。实用建议：

1. 先用平台默认采样器。
2. 想提速，试「少步数快采样器」类（平台常标注）。
3. 想要更干净的图，试「多步数稳采样器」类。
4. 同一提示换采样器，观感会有微妙差别，挑顺眼的固定。

记住：采样器影响「质感调性」，提示词和引导强度影响「内容对错」，权重不同。

---

## 七、训练直觉：模型到底「学过」什么

要理解模型的能力边界，得想清楚它训练时「见过」什么：

- 它看过海量「图 + 文字说明」配对数据，学到的是**统计对应关系**，不是规则手册。
- 所以它擅长「常见组合」：猫+草帽+海边，这类数据极多，表现好。
- 它弱于「罕见或矛盾组合」：比如「七条腿的桌子」「文字精确排版」，数据少或本身矛盾，易翻车。
- 它容易复制训练数据里的「偏见」：比如某些风格过度关联某类主体。使用者要有意识校正。

这给我们的行动指南：**顺着数据的常见分布写提示，成功率最高**。想做非常规创意时，用图生图或多轮迭代逐步逼出，而不是一句提示硬塞。

---

## 八、失败模式解析：为什么手指总是错

新手最困惑的「手指出错」其实有原理：

人体手在训练图里姿态千变万化，且手指细、易被压缩丢失。模型在潜空间里本就难精确表达「五根手指的拓扑」，去噪时容易数错或扭曲。同理，以下也常出错：

| 易错项 | 原因 | 缓解办法 |
| --- | --- | --- |
| 手指/脚趾数量 | 细节小、拓扑复杂 | 负面提示加「多余手指」，或后期修 |
| 图中的文字 | 文字非自然图像分布 | 后期加文字，或负面提示禁文字 |
| 对称物体（眼镜、鞋） | 左右难对齐 | 局部重绘修正 |
| 远距离小人脸 | 分辨率不够承载 | 拉近主体或后期放大修 |

理解「错在哪、为何错」，你就不会恐慌，而是有策略地绕开或修补。

---

## 九、分辨率与比例：构图是被尺寸影响的

很多人以为分辨率只影响「清不清晰」，其实它还影响**构图**：

- 正方形易把主体居中、对称；
- 竖版天然适合单人、全身、向上延伸；
- 横版适合场景、风景、多人互动。

模型对不同比例有「偏好」，强行用怪异比例可能出畸变。建议：

1. 先定用途（封面/配图/壁纸）再定比例。
2. 用平台推荐的常见比例（如 1:1、3:2、2:3、16:9）。
3. 超高分辨率（如远超平台训练尺寸）可能让主体变小、细节碎，不必盲目拉满。

---

## 十、推理成本：时间、算力与钱的关系

每次生成都消耗算力，理解成本结构能帮你省钱：

- **成本 ≈ 分辨率 × 步数 × 张数**。任一翻倍，成本大致翻倍。
- 所以「先小图低步数筛构图，再大图高步数出成品」是核心省钱法。
- 批量生成时，用「变化种子」而非「全参数重跑」，有时更省。
- 本地部署能把钱换成电费和时间，但需设备与配置门槛，初学不必。

举个具体账：假设单张基准成本系数为 1（中分辨率 + 30 步）。若把分辨率翻倍（边长×2≈面积×4），成本约 ×4；再把步数从 30 提到 60，再 ×2；再生成 4 张，×4。总系数约 1×4×2×4 = 32 倍。也就是说，「直接高分辨率高步数批量出成品」比「小图低步数筛 4 张再选 1 张大图精修」可能贵出十几倍。这就是为什么老手都先「小图试错」。

---

## 十一、本章提示词示例

沿用四段式。在主流文生图平台使用以下提示词：

```
主体：一位戴圆框眼镜的年轻学者，手持古籍，神情专注
场景：暖光书房，木质书架环绕，窗外飘着细雪
风格：写实厚涂，电影感，细腻笔触
光线镜头：窗边侧逆光，面部柔和补光，中景，浅景深
质量：高细节，皮肤质感真实，8K，构图平衡，无畸变
```

试着把「步数」从 20 调到 40，观察边缘与纹理变化；再把「引导强度」从 9 调到 5，观察画面是否更「自由」但可能偏题。这就是把原理变成手感。

---

## 十二、参数说明表（本章涉及的）

| 参数 | 含义 | 建议区间 | 调高的影响 |
| --- | --- | --- | --- |
| 步数 Steps | 去噪迭代次数 | 20–40 | 更精细，但过犹不及、更慢 |
| 引导强度 CFG | 提示服从度 | 7–12 | 更贴提示，但易死板过饱和 |
| 分辨率 | 输出尺寸 | 平台推荐比例 | 更清晰，但更慢更贵、可能畸变 |
| 采样器 | 去噪算法 | 默认起 | 影响质感调性 |
| 种子 Seed | 随机起点 | 任意 | 固定可复现 |

---

## 十三、常见错误与避坑清单

1. **步数拉满以为更好**：超过甜区后收益骤减，纯浪费。
2. **引导强度过高**：图变「塑料感」、过饱和、构图僵硬。
3. **忽视比例导致畸变**：用怪比例强出图，主体被拉变形。
4. **不懂原理就乱换采样器**：换来换去不知好坏，先固定默认。
5. **把失败当「模型差」**：多数失败是提示/参数不当，先排查再归因。
6. **直接在最高分辨率试构图**：烧钱且慢，应先小图筛。

---

## 十四、进阶技巧

- **锁定变量法**：一次只改一个参数，观察其单独影响，建立「参数—效果」映射。
- **种子淘金**：生成一批后，挑最接近的一张固定种子，再微调提示做变体。
- **低步数预览流**：工作流里用 15 步小图快速迭代提示，定稿才上 30+ 步。
- **理解潜空间局限**：遇到文字/精确结构需求，果断交给后期工具，不在生成阶段死磕。
- **建立私人参数档案**：把你常用场景的「最佳步数/CFG/比例」记下来，下次直接套，省去重复试错。
- **关注模型更新日志**：平台常优化采样与默认参数，新模型可能改变甜区，定期回测你的默认值。
- **用对比图做笔记**：把同提示不同参数的图并排存，标注差异，这是比文字更牢的记忆方式。

---

## 十五、实战小案例：理解参数的「受控实验」

目标：通过一次对比，亲手感受步数与引导强度的作用。

步骤：
1. 用第十一节提示词，固定其他参数。
2. 第一组：步数 20 / CFG 9，生成 2 张，记为 A。
3. 第二组：步数 40 / CFG 9，生成 2 张，记为 B。
4. 第三组：步数 30 / CFG 5，生成 2 张，记为 C。
5. 对比 A 与 B：B 是否更干净？差异是否值得多花的时间？
6. 对比 A 与 C：C 是否更自由但可能偏题？

结论记录：写下你观察到的规律，作为以后调参的私人手册。

---

## 十六、合规、版权与成本注意点

**合规**：原理层面的理解不改变合规义务——不生成受限内容，遵守平台条款。

**版权**：你理解了「模型从训练数据学分布」，更要意识到生成图可能无意中贴近某些已有作品的风格。商用前评估相似度风险，必要时做差异化改造。

**成本**：基于本章成本公式，养成「小图筛、大图出」习惯；批量任务预算前先算张数×分辨率×步数。

---

## 十七、本章小结

回看目标，你现在应能解释：
- 扩散=逐步去噪，生成是「从噪声恢复结构」；
- 潜空间为效率而存在，代价是极细信息易失；
- 文字经编码成向量，「指挥」每步去噪；
- 引导强度权衡服从与自由，步数决定精细度，采样器定调性。

这些就是后面所有操作的「底层逻辑」。

---

## 十八、原理如何反哺提示词写作

理解了原理，写提示词会有「上帝视角」。举几个直接推论：

- **因为文字被编码成向量，描述越连贯具体，向量指向越准**：与其堆「猫，可爱，萌」，不如写「一只蜷在毛毯里、半眯眼打盹的橘猫」——后者给模型更清晰的视觉锚点。
- **因为模型顺常见分布走**：把罕见创意拆成「已知元素 + 已知关系」逐步叠加，比塞一句矛盾描述更易成功。
- **因为潜空间丢极细信息**：需要清晰文字/标志时，老老实实后期加，别在提示里硬刚。
- **因为引导强度控服从度**：当你要「自由艺术感」，敢把 CFG 调低；要「精确还原草图」，敢调高。

这一节是承上启下的：下一章的「提示词结构」，本质就是把这里的原理变成可复用的写法。

---

## 十九、常见疑问 FAQ

**Q1：模型真的「知道」猫长什么样吗？**
它知道「训练数据里猫的图像统计长啥样」，不是生物意义上的知道。所以它能画猫，但画不准解剖细节。

**Q2：为什么有时加一个词效果翻天覆地？**
那个词可能精准命中了模型里一个强向量（如某画派、某镜头），一下把整段语义拉向某个区域。

**Q3：步数越高越真实吗？**
不一定。真实感更多来自提示（材质词、光线词）与模型本身，步数只负责把已定的结构做干净。

**Q4：我能看到潜空间里的内容吗？**
一般不能，它是抽象数学表示。你看到的只是最终解码出的图。

**Q5：本地部署和云端生成原理一样吗？**
一样，只是算的地方不同。本地用自己设备，云端用平台服务器。

---

## 二十、上手练习

1. 用大白话向朋友解释「扩散模型是怎么从噪声变出图的」，录音或写 100 字。
2. 在主流平台用同一提示，分别设 CFG=7 与 CFG=14 各生成 2 张，描述差异。
3. 解释「为什么模型擅长常见组合、弱于罕见组合」，并举一你遇到的或能想到的例子。

---

## 二十一、原理速查卡（一页纸）

把本章浓缩成一张可随时回看的卡：

- **扩散 = 逐步去噪**：从噪声恢复结构，不是凭空画像素。
- **步数**：去噪次数，20–40 甜区，过高无益。
- **潜空间**：为效率压缩，代价是极细信息（文字/标志）易失。
- **文本编码**：提示词变向量，连贯具体 > 堆词。
- **引导强度 CFG**：服从提示 vs 自由发挥，7–12 常用。
- **采样器**：去噪算法口味，先默认后尝试。
- **训练分布**：顺常见组合走，罕见/矛盾组合易翻车。
- **失败根源**：手、文字、对称物因细节小或拓扑复杂而易错。
- **成本公式**：分辨率 × 步数 × 张数。
- **核心心法**：小图筛构图，大图出成品；一次只改一个变量。

打印或截图这张卡，调参迷茫时看一眼，立刻回神。

---

## 二十二、从原理到审美：为什么好图还要人把关

懂原理能让你「不翻车」，但「不翻车」不等于「好看」。模型在潜空间里恢复的是「统计上合理的图」，而审美是主观的、有意图的、带叙事的。举个例：

- 原理保证「猫的手指（爪）数量正常」，但保证不了「这只猫的姿态是否传达孤独」。
- 原理保证「光线计算正确」，但保证不了「这束光是否引导视线到你想要的焦点」。
- 原理保证「风格词生效」，但保证不了「这种风格是否契合你账号的整体调性」。

所以，原理是地基，审美是上层。本章给你的，是把「想要」准确翻译成「参数与提示」的能力；至于「想要什么」，靠你在第一章提到的构图、色彩、叙事积累。两者结合，才是从「能用」到「好用」的跃迁。

---

> 下一章进入「文生图提示词基础」，我们会把本章的原理，落实成你每天要写的那些词——怎么组织、怎么写清主体、怎么定风格。
