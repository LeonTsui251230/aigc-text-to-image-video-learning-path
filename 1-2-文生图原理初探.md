# 1-2 文生图原理初探

不堆公式，只建立直觉。

## 从噪声到图像

主流文生图模型使用扩散思想：先准备一张纯噪声图，模型一步步「去噪」，每步都让画面更接近你的文字描述，直到出现清晰图像。

## 文字如何变成画面

模型在训练时见过大量「图文对」，学会了把词语和视觉特征关联。你输入「夕阳、海边、长曝光」，模型就在内部激活对应的视觉模式。

## 为什么同一句话每次结果不同

生成起点是随机噪声，每一步也有随机性。所以「种子（seed）」这个概念很重要：固定种子，就能复现同一结果，方便迭代。

## 潜空间是什么

模型不在原始像素上工作，而是先把图压缩到一个更紧凑的内部表示（潜空间），在那里生成再解压回来。这也是为什么生成快、且能做「在潜空间里插值」得到渐变效果。

## 上手练习

找一个平台，用同一句话、不同种子生成 4 张图，观察差异，体会随机性的存在。
