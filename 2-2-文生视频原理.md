# 文生视频原理

## 本章定位与学习目标

上一章你把静态图后期玩明白了，这一章进入「会动的图」——文生视频。很多人以为文生视频就是「文生图加个时间轴」，其实远没那么简单：让一张图动起来不难，难的是让它在几秒内不抽搐、不变形、不凭空冒出第六根手指。学完本章，你要建立起对文生视频底层逻辑的直觉：它到底在生成什么、最大的难点在哪、为什么同样的提示词有时好有时崩，以及哪些参数真正影响成片观感。不堆公式，只讲能指导你动手的认知。

---

## 一、核心概念：视频就是「一叠按顺序排列、彼此相似的图」

先把视频拆开看本质。一段 3 秒、每秒 24 帧的视频，本质就是 72 张图，排着队快速翻过去。人眼因为「视觉暂留」，把快速切换的静帧看成连续运动。

所以文生视频要做的事，可以朴素地理解为：

> **生成一堆「内容一致、但每一张比上一张挪了一点点」的图，再按顺序播出来。**

难点立刻浮现：这 72 张图里，猫必须一直是那只猫，杯子不能突然消失又出现，光线不能忽明忽暗。只要相邻两张图「挪得不对」或「长得太不像」，播出来就是闪烁、跳变、抽搐。

这就是文生视频和文生图最根本的区别——**文生图只管「一张对不对」，文生视频还要管「72 张之间连不连」**。多出来的这个「连」，就是全部难点的来源。

---

## 二、从文生图到文生视频：多出来的「时间」维度

文生图模型在「图像空间」里工作：输入文字，输出一张图。文生视频模型多了一个维度——时间。它要同时回答两个问题：

1. **空间上**：这一帧长什么样？（和文生图类似）
2. **时间上**：下一帧相对这一帧，东西该怎么动？（文生图完全不涉及）

直觉上可以这样想：文生图是「画一个瞬间」，文生视频是「导演一段连续的瞬间」。模型内部不仅要理解「画面里有只猫」，还要理解「猫的尾巴会这样摆、耳朵会那样动、身体重心如何转移」。

> 关键认知：**文生视频 = 文生图能力 + 时间连贯能力**。前者决定「画什么」，后者决定「怎么动得自然」。很多成品翻车，不是画得不好，而是动得不自然。

---

## 三、最大难点：时间一致性（帧间连贯）

「一致性」是文生视频第一章就该刻进脑子里的词。它包含好几层含义：

| 一致性类型 | 翻车表现 | 原因直觉 |
| --- | --- | --- |
| 主体一致性 | 猫变狗、人脸每帧变 | 每帧独立生成，没记住「这只猫」 |
| 运动一致性 | 物体瞬移、闪现消失 | 帧间位移算错 |
| 光照一致性 | 忽明忽暗、阴影乱跳 | 没约束光的方向 |
| 场景一致性 | 背景元素位置乱跳 | 空间记忆不稳 |
| 风格一致性 | 帧间画风漂移 | 风格没锁死 |

为什么难？因为模型生成每一帧时，本质是「在噪声里重新采样」。如果没专门设计「让相邻帧共享信息」的机制，每帧就会各画各的，结果就是闪烁和变形。

现代模型用各种办法缓解：让前后帧共享潜变量、用运动场约束位移、用参考帧锚定主体等。但你要知道——**一致性是概率性的，不是保证性的**。所以同样的提示词，有时出片稳，有时崩，根子在这。

---

## 四、主流技术路线的直觉（不背名字，懂思路）

不必记具体模型，但懂几条路线能帮你预判「它擅长什么、怕什么」。

### 路线 A：扩散式视频生成

和文生图的扩散思路一脉相承：从一团噪声开始，逐步去噪，但去噪过程同时考虑「时间相邻帧」。可以理解为「一边画，一边保证前后帧对得上」。

- 擅长：单段短镜头、运镜自然。
- 弱点：长视频容易中段失控，需要分段生成再拼。

### 路线 B：潜空间视频（先在压缩空间生成，再解码）

先把视频压到一个「更小但保留核心信息」的隐空间里生成运动和内容，再放大还原成清晰视频。好处是省算力、运动更稳；代价是细节要靠后期超分补。

### 路线 C：运动先验 / 光流引导

先预测「画面里每个点往哪动」（运动场/光流），再按这个运动去生成帧。像先画好「动作说明书」，再让画面照着演。运动可控性更强。

### 路线 D：自回归逐帧生成

像写文章一样，生成第 N 帧时参考第 N-1 帧。连贯性好，但容易「误差累积」——越往后越偏，需要定期用参考帧拉回。

> 你不用选路线，平台背后用的是哪条和你无关。但知道「连贯是概率的、长片要分段」，能让你少踩坑。

---

## 五、提示词如何被「翻译」成运动

你写的文字，模型怎么变成「动」？直觉上分两步：

1. **解析语义**：从提示词里抽出「主体、场景、动作、镜头」等要素。
2. **映射成运动指令**：把「走、飘、旋转、下雨」这类词，对应到潜空间里「帧间该怎么变」。

举例：

| 你写的词 | 模型大致做的 |
| --- | --- |
| 一朵花缓缓绽放 | 帧间让花瓣角度渐进张开 |
| 镜头缓慢推近 | 帧间让主体占比渐进变大 |
| 微风拂过发丝 | 帧间让发丝位置小幅飘动 |
| 海浪拍打礁石 | 帧间让水花位置与形态循环变化 |

注意：**动作词越具体，运动越可控**。写「动一下」模型不知道怎么动；写「从左向右慢速平移」模型才有明确目标。这直接决定下一章提示词怎么写。

---

## 六、关键参数：真正影响观感的几个旋钮

文生视频平台通常给这些参数，理解它们比乱调重要：

| 参数 | 含义 | 建议 |
| --- | --- | --- |
| 时长 | 生成多少秒 | 新手 3~5 秒，稳；长片分段 |
| 帧率 (fps) | 每秒几帧 | 24 电影感，30 流畅，高帧率更顺但吃算力 |
| 分辨率 | 画面尺寸 | 对标发布平台，别盲目拉满 |
| 运动幅度 | 动作大不大 | 小幅度稳，大幅度易崩 |
| 随机种子 | 随机起点 | 固定种子可复现、便于迭代 |
| 运动平滑度 | 帧间过渡软硬 | 高更顺，低更有顿挫/定格感 |

**运动幅度**是最该理解的：它不是「动得多快」，而是「每帧相对上一帧变化多大」。幅度大 = 每帧差异大 = 更容易露出不一致；幅度小 = 稳但可能「看着没动」。新手建议从中低幅度起步，确认稳了再加大。

**种子**是隐藏神器：固定种子，你能在「同一底随机」上微调提示词，对比哪句提示词有效，而不是每次都换一批完全不同的随机结果。

---

## 七、常见产物问题与根因

看懂这些症状，你才能对症下药：

| 现象 | 俗称 | 可能原因 |
| --- | --- | --- |
| 画面高频闪烁 | 雪花/抽搐 | 帧间一致性不足，幅度过大 |
| 物体凭空消失又出现 | 闪现 | 空间记忆丢失 |
| 肢体扭曲变形 | 橡皮人 | 运动超出模型能力 |
| 镜头乱跳不连续 | 跳切 | 运镜指令冲突或幅度过大 |
| 越往后越怪 | 漂移 | 误差累积（自回归类） |
| 主体每帧换脸 | 变脸 | 没锚定参考帧 |

> 诊断口诀：**闪 = 幅度/一致性问题；变 = 没锚定主体；漂 = 片段太长。** 下一章会讲怎么在提示词层面规避。

---

## 八、提示词示例（通用结构）

在主流视频生成平台使用以下提示词结构（按「主体 / 场景 / 动作 / 镜头 / 风格 / 质量」组织）：

```
主体：一只橘色小猫，圆脸大眼，毛色蓬松
场景：阳光洒落的木质窗台，背后是虚化的绿植
动作：小猫缓缓转头看向镜头，尾巴轻轻摆动，耳朵微动
镜头：固定机位中景，极缓慢推近，无大幅度运镜
风格：温暖治愈系写实，柔光，浅景深
质量：电影感，细节清晰，运动平滑，帧间稳定
```

要点：动作写「缓慢、轻、渐进」，镜头写「固定/极缓」，给模型「稳」的指令，比一上来就「高速旋转爆炸」成功率高得多。

再给一版「氛围感」示例，体会用词差异：

```
主体：一盏纸灯笼
场景：夜晚安静的古镇街道，水面倒影
动作：灯笼随微风轻轻摇晃，水面倒影同步波动
镜头：固定机位远景，几乎不动
风格：东方写意，冷蓝月光，柔和噪点质感
质量：电影感，帧间极度稳定，循环可无缝
```

这版几乎全是模型擅长的「轻摆、流体、缓动」，成功率天然高。对比上一版你应已感觉到：把需求落到「擅长区间」，是文生视频的核心心法。

---

## 九、参数与模式说明（实操建议）

- **新手起步模式**：3 秒 + 24fps + 中低运动幅度 + 固定种子。先求「稳」，再求「炫」。
- **运镜模式**：推、拉、摇、移、环绕，各选其一，别叠加。多运镜同时下令模型顾不过来。
- **循环/无缝模式**：部分场景（烟、水流、火焰）可生成首尾相接的循环片段，适合做背景素材。
- **首尾帧模式**：指定第一帧和最后一帧，让模型只生成中间过渡。这是控制一致性的最强手段（详见 2-4、2-6）。

---

## 十、常见错误与避坑清单

1. **一上来就大幅度**：写「高速飞行、剧烈爆炸」，必崩。先小幅度验证。
2. **运镜叠太多**：推+摇+环绕一起写，镜头乱跳。一次一个运镜。
3. **片段太长**：直接要 30 秒，中段必失控。拆 3~5 秒分段再拼。
4. **不固定种子**：每次随机，无法对比提示词效果。迭代时固定种子。
5. **动作词太虚**：「动一下」「有活力」模型无从下手。写具体方向幅度。
6. **忽视首帧**：文生视频首帧随机，主体可能不理想。需要可控主体时改用图生视频（2-4）。
7. **分辨率盲目拉满**：4K 视频吃算力又慢，先看平台主流档位。
8. **拿视频当图用**：要单张好图，回去文生图，别从视频里截（分辨率低）。

---

## 十一、进阶技巧

### 1. 分段生成 + 拼接

长叙事拆成若干 3~5 秒镜头，各自生成，再用剪辑软件按脚本拼。每段短，一致性好控；拼起来就是长片。这是目前最稳的「长视频」做法。

### 2. 固定种子迭代法

固定种子，只改提示词里一个变量（如把「尾巴轻摆」改成「尾巴大幅摆」），观察差异。像做实验，快速找到有效写法。

### 3. 用参考帧锚定主体

能上传首帧/参考图的，务必上传。把「文生视频」变成「以图为基础的视频」，主体一致性立刻上一个台阶（2-4 详解）。

### 4. 运动幅度梯度测试

同一提示词，分别用低/中/高幅度各生成一次，挑最自然的那档作为基线，后续沿用。

### 5. 抽帧检查一致性

生成后逐帧暂停看：主体有没有变、背景元素有没有跳。发现问题帧，针对性用图生视频修补该段。

---

## 十二、实战小案例：一段 3 秒产品展示

**目标**：生成一段手机在桌面缓慢旋转展示的 3 秒短片，要求主体稳定、无闪烁。

**做法**：
1. 参数：3 秒、24fps、中低运动幅度、固定种子。
2. 提示词主体写清手机颜色造型；动作写「极缓慢自转，约 30 度」；镜头写「固定俯拍中景」。
3. 先低幅度试一版，确认不闪不变形，再微调幅度到满意。
4. 抽帧检查：每帧手机造型一致、桌面不变，达标。

**验收**：播放流畅、主体始终如一、无抽搐闪烁，可直接用于封面动效或短视频开头。

---

## 十三、成本与合规注意点

- **算力成本**：视频比图片贵得多（要算几十上百帧）。时长、分辨率、帧率每加一档，成本指数上升。新手先用短小低清试错。
- **生成失败率**：视频废片率高于图片，要有「多生成几版挑一版」的心理和预算预期。
- **内容合规**：生成的暴力、敏感、侵权内容同样受平台与法律约束（详见 2-7）。
- **声音**：纯文生视频通常不含音轨，配乐/配音需另做，注意音乐版权。

---

## 十三（补）、运镜与动作词汇速查

提示词写「动」和「镜头」时，用对词能省一半试错。下面是可抄的词汇表：

**动作幅度词**（告诉模型每帧变化多大）：
- 极缓 / 缓慢 / 轻微 / 小幅：稳定优先，新手默认。
- 中等 / 自然：日常动作。
- 大幅 / 剧烈 / 快速：高风险，易崩，留到主体锚定后试。

**常见动作词**：
- 平移：从左向右、由远及近、横向滑过。
- 旋转：自转、环绕、翻滚（环绕/翻滚尤其吃一致性）。
- 形变：绽放、展开、溶解、聚合。
- 流体：升腾、流淌、飘落、拍打、涟漪。
- 生物：眨眼、摆尾、呼吸起伏、行走、转头。

**运镜词**（一次只选一个）：
- 固定机位：最稳，适合产品/口播。
- 推近 / 拉远：主体占比变化。
- 平移 / 横移：机位平行移动。
- 摇镜头：机位原地转。
- 环绕：绕主体转，最考验一致性。
- 俯拍 / 仰拍 / 平视：视角高度。

> 写法铁律：**动作 + 方向 + 幅度**三要素齐全，例如「尾巴从左向右小幅轻摆」，比「尾巴动」可控十倍。

---

## 十三（补二）、模型的运动能力边界

理解模型「天生不擅长」什么，能让你避开必崩区：

| 难做的运动 | 为什么难 | 规避办法 |
| --- | --- | --- |
| 复杂手部动作 | 手结构精细，易变橡皮人 | 手入镜少、或后期替换 |
| 准确计数物体 | 数不清几个 | 避免「五个杯子依次倒下」 |
| 文字/招牌稳定 | 文字会糊会变 | 视频里少放文字，后期加 |
| 长时间行走 | 步态累积误差大 | 短段 + 拼接，或图生视频 |
| 物理精确（碰撞） | 不懂真实物理 | 别指望精确碰撞特效 |
| 多人一致对话 | 多人各自动易乱 | 单人为主，多人用参考帧 |

一句话：**模型擅长「氛围感运动」（飘、流动、轻摆、缓转），不擅长「精确物理运动」（计数、碰撞、复杂肢体）**。把需求落到它擅长的区间，成功率直线上升。

---

## 十三（补三）、时间一致性的工程直觉（深入一点）

如果你好奇「模型到底怎么记住上一帧」，给个极简直觉，不背公式：

- 它内部维护一个「当前画面状态」，生成下一帧时，不是从零开始，而是基于这个状态做「小幅更新」。
- 运动指令相当于「更新规则」：告诉状态「往哪个方向改多少」。
- 一致性好 = 更新规则平滑、没突变；一致性崩 = 某帧更新过了头，状态跳变，后续帧全偏。

所以你写提示词时，凡是让「更新规则突变」的写法（大幅度、多运镜冲突、主体突然消失）都会触发崩。反过来，平缓、单一、明确的指令，就是给模型「平滑更新」创造最好条件。

这也能解释为什么「固定种子 + 小步微调」是最稳的迭代法：每次只动一点点更新规则，问题好定位、好复现。

---

## 十三（补四）、文生视频 vs 图生视频：什么时候用哪个

本章讲「纯文字生成视频」，但下一章会讲「给一张图生成视频」。提前给决策依据：

| 你的起点 | 选文生视频 | 选图生视频 |
| --- | --- | --- |
| 只有文字想法，没图 | ✅ | ❌ |
| 已有满意的首帧图 | ❌ | ✅（一致性更强） |
| 主体必须精确可控 | ❌（随机） | ✅（锚定首帧） |
| 只想快速试一个动态概念 | ✅ | ❌（还需先出图） |
| 做系列视频要统一主角 | ❌ | ✅ |

记忆点：**文生视频适合「从 zero 试创意」，图生视频适合「已有好图要它动起来」**。两者互补，不是替代。

---

## 十四、本章小结与承上启下

- 视频 = 一叠连续相似的图，「连不连」是核心难点，即时间一致性。
- 文生视频 = 文生图 + 时间连贯，翻车多在「动得不自然」而非「画得不好」。
- 关键参数里，运动幅度和种子最该先搞懂；新手求稳，别一上来拉满。
- 长片靠分段生成 + 拼接，一致性靠参考帧锚定。

下一章（2-3）我们专门练「怎么把运动写进提示词」，把这一章的原理变成可抄的句子。

> 一句话带走本章：文生视频难不在「画」，在「连」。你写给模型的每句话，本质都是在约束「下一帧相对这一帧该怎么变」。把这句话记牢，后面所有章节都会顺。

---

## 十五、上手练习

1. 在主流视频生成平台，用「固定种子 + 中低幅度」生成一段「咖啡杯上热气缓缓升腾」的 3 秒视频，抽帧确认无闪烁。
2. 同一主体，分别写「大幅度旋转」和「极缓慢旋转」各生成一版，对比哪版一致性更好，记录结论。
3. 把一段 10 秒的设想拆成 3 个 3 秒镜头脚本（含每镜的主体、动作、镜头），体会「分段生成」的规划思路。
4. 任选一个「模型不擅长」的动作（如多人精确对话），生成一版观察它如何翻车，再改成一个「擅长」的动作（如烟雾缓缓飘动）对比，理解能力边界。
