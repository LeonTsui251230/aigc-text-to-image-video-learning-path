# 负面提示与参数调优

## 本章定位与学习目标

前面四章你都在写「想要什么」（正向提示）。本章补齐另一半：写「不想要什么」（负面提示），并系统讲清楚每个参数怎么调、调优该按什么顺序走。学完本章，你应能独立把一张「糊、乱、有瑕疵」的图，一步步调成「干净、可控、可用」。

负面提示往往被新手忽略，但它对成图质量的影响，经常不亚于正向提示。参数调优则是把「概率性成功」变成「稳定成功」的关键。

---

## 一、核心概念：负面提示是在「划禁区」

模型每一步去噪时，除了听正向提示「往这靠」，还能听负面提示「别往那去」。你可以把负面提示理解为给画师的一张「避坑清单」：

> 「手别画多指、背景别糊成一团、别出现乱码文字、别过饱和。」

它不改变你想要的，只排除你不想要的。尤其在默认参数下，模型容易冒出多余物体、畸变、文字乱码，负面提示能大幅减少这些。

> 关键认知：**正向提示决定「画什么」，负面提示决定「别画什么」**。两者配合，图才干净。

---

## 二、为什么需要负面提示：模型的「常见毛病」

模型天生有几个高频毛病，几乎每张都该防：

| 毛病 | 表现 | 负面词方向 |
| --- | --- | --- |
| 多余手指/肢体 | 六指、断指、多手 | 多余手指、畸形手 |
| 乱码文字 | 图中冒出扭曲字母/汉字 | 文字、签名、水印 |
| 畸变 | 脸歪、物体融化 | 畸变、变形、解剖错误 |
| 低质 | 糊、噪点、锯齿 | 模糊、低分辨率、噪点 |
| 过饱和 | 颜色艳俗 | 过饱和、高对比假 |
| 多余物体 | 背景乱入不相干东西 | 多余物体、杂乱 |

负面提示就是把这些「常见病」提前屏蔽。

---

## 三、通用负面词库（可直接套）

下面是一份通用负面清单，多数场景都能用（具体词依平台习惯，可中英混用）：

```
模糊，低分辨率，噪点，锯齿，畸变，变形，解剖错误，
多余手指，多手，多脚，多余肢体，
乱码文字，签名，水印，logo，
过饱和，色彩失真，塑料感，
杂乱背景，多余物体，构图失衡，
丑陋，比例失调
```

使用建议：把它存为「默认负面」，每次生成都带上，再按场景叠加专项负面词。这样基础质量有保障。

---

## 四、按场景加专项负面词

不同场景有不同雷区，叠加更精准：

| 场景 | 加的专项负面词 | 为什么 |
| --- | --- | --- |
| 人像 | 丑陋面部，不对称眼睛，戴错眼镜 | 人脸最易被盯，瑕疵明显 |
| 产品图 | 阴影杂乱，反光过度，背景脏 | 产品要干净高级 |
| 文字相关（后期加字处） | 文字，标签 | 防止背景冒乱码抢视觉 |
| 插画/动画 | 写实照片感（若要纯画风） | 防止风格被照片感拉走 |
| 夜景 | 过曝，白天感 | 保夜调 |
| 极简 | 多余装饰，复杂纹理 | 保简洁 |

原则：专项负面词越少越准越好，别把整本词典塞进去——负面词过多也可能干扰正向。

---

## 五、负面提示的写法细节

1. **与正向同样用逗号分隔**，逐条列出。
2. **权重也可用**：部分平台支持给负面词加权，如 `(多余手指:1.2)`，强化排除。
3. **不要和正向矛盾**：正向写「高清」，负面就别写「模糊」这种反向——其实写了也无妨，但保持清晰更好：正向说要的，负向说不要的，各司其职。
4. **平台有默认负面时**：有些平台已内置基础负面，你只需补专项，避免重复堆叠。

---

## 六、参数总览：五个旋钮分别管什么

| 参数 | 管什么 | 低→高趋势 | 新手建议 |
| --- | --- | --- | --- |
| 步数 Steps | 去噪精细度 | 糊→清→稳 | 20–40 |
| 引导强度 CFG | 听提示程度 | 自由→服从→死板 | 7–12 |
| 分辨率 | 清晰度与构图承载 | 糊→清→畸变风险 | 平台推荐 |
| 采样器 | 去噪算法口味 | 快糙→慢稳 | 默认 |
| 种子 | 随机起点 | 固定可复现 | 任意 |

下面逐参数讲「怎么调」。

---

## 七、步数（Steps）：精细度的旋钮

- **太低（<15）**：结构未定型，图糊、细节缺。
- **适中（20–40）**：多数场景甜区，干净且不太慢。
- **太高（>50）**：收益骤减，更慢更贵，偶尔反而过平滑。

调优法：先用 25 出小图看构图；定稿用 30–35。若图仍糊，先查 CFG 与提示，别只加步数。

---

## 八、引导强度（CFG）：服从度的旋钮

- **低（<6）**：自由发散，易跑题、主体弱。
- **适中（7–12）**：贴合又自然，最常见。
- **高（>15）**：极度服从但易过饱和、锐化假、构图僵。

调优法：觉得「没按说的来」→ 加 1–2；觉得「太死板/塑料」→ 减 1–2。不要一拉到顶。

---

## 九、分辨率与比例：清晰度与构图的旋钮

- 分辨率决定清晰上限，但超高分辨率（远超训练尺寸）会让主体变小、细节碎、出畸变。
- 比例影响构图（见 1-2 章）：先定用途再定比例。
- 调优法：用途是封面→竖版；配图→横版；头像→方。分辨率用平台推荐档，别盲目拉满。

---

## 十、采样器：质感的旋钮

- 不同采样器观感微妙不同，先固定默认。
- 想提速试「少步快采样」类；想要更干净试「多步稳采样」类。
- 调优法：同一提示换 2–3 个采样器各出一张，挑顺眼固定，不必深究数学。

---

## 十一、种子：复现与探索的旋钮

- 随机种子=每次不同；固定种子=复现某张。
- 调优法：生成一批，挑最接近的一张，固定其种子，再微调提示/参数做变体。这比全随机高效。

---

## 十二、系统调优路径：从「糊」到「精」

给一套可复用的顺序，避免瞎调：

1. **先定正向提示**（四段式写清），负面用通用默认。
2. **小图低步数（20 步）筛构图**：看主体、场景、风格对不对。
3. **构图不对** → 改正向提示（主体/场景/构图词），重复 2。
4. **构图对、细节糊** → 步数提到 30–35，CFG 保持 9。
5. **跑题** → CFG +1~2；**死板** → CFG -1~2。
6. **有瑕疵**（手指/文字/畸变） → 补专项负面词。
7. **挑中最接近的一张，固定种子**，微调出 2–3 变体。
8. **大图高步数出成品**（分辨率用推荐档）。

这套顺序的核心是：**先内容后质量，先大方向后细节，一次只动一个变量**。

---

## 十三、实战调优记录表（建议自建）

每次调优记下来，积累成私人手册：

| 轮次 | 改了什么 | 结果 | 结论 |
| --- | --- | --- | --- |
| 1 | 初稿 25 步 CFG9 | 构图偏左 | 加「居中」 |
| 2 | 加居中词 | 构图好，手多指 | 负面加「多余手指」 |
| 3 | 负面加权 | 手正常，略糊 | 步数 35 |
| 4 | 步数 35 | 干净，定稿 | 固定种子出变体 |

这种「实验日志」比脑记可靠得多，是进阶者的标配习惯。

---

## 十四、负面提示示例（通用版）

在主流文生图平台，将以下作为负面提示：

```
模糊，低分辨率，噪点，锯齿，畸变，变形，解剖错误，
多余手指，多手，多脚，多余肢体，
乱码文字，签名，水印，logo，
过饱和，色彩失真，塑料感，
杂乱背景，多余物体，构图失衡，丑陋，比例失调
```

若做人像，再补：`不对称眼睛，丑陋面部，戴错眼镜`；若做产品图，再补：`阴影杂乱，反光过度，背景脏`。

---

## 十五、参数说明表（本章汇总）

| 参数 | 含义 | 建议区间 | 调高影响 |
| --- | --- | --- | --- |
| 步数 | 去噪次数 | 20–40 | 更细，过则慢且无益 |
| CFG | 提示服从度 | 7–12 | 更贴，过则死板 |
| 分辨率 | 输出尺寸 | 平台推荐 | 更清，过则畸变 |
| 采样器 | 去噪算法 | 默认 | 影响质感 |
| 种子 | 随机起点 | 任意 | 固定可复现 |
| 负面权重 | 排除强度 | 1.0–1.3 | 更强排除，过则干扰 |

---

## 十六、常见错误与避坑清单

1. **完全不用负面提示**：图常带手指/文字/畸变瑕疵。
2. **负面词堆成词典**：干扰正向，反降质量。
3. **只加步数救糊图**：糊可能因 CFG 低或提示空，应先排查。
4. **CFG 拉到 20**：过饱和塑料感。
5. **超高分辨率强出**：畸变且贵。
6. **一次改多个参数**：不知谁起作用，无法定位。
7. **不固定种子就迭代**：每次全变，难收敛。

---

## 十七、进阶技巧

- **负面权重分级**：核心毛病（手指/畸变）加权到 1.2，次要（水印）保持 1.0。
- **按平台调负面**：不同平台「常见病」不同，观察你的图最常出什么，建专属负面。
- **CFG 与负面联动**：CFG 高时模型更服从，负面也更有效；可配合微调。
- **模板化**：存「通用负面 + 人像负面 + 产品负面 + 插画负面」四套，随场景切换。

---

## 十八、实战小案例：修一张「六指猫」

目标：把一张主体可爱但「多指、背景糊、略过饱和」的猫图调干净。

步骤：
1. 正向提示保持（橘猫戴草帽海边插画）。
2. 负面加：`多余手指，多手，畸变，过饱和，模糊`。
3. 步数 25→32，CFG 保持 9。
4. 生成，若手指仍偶发，负面 `(多余手指:1.3)` 加权。
5. 挑一张固定种子，出 2 变体。
6. 若背景仍糊，加负面 `杂乱背景` 或正向加 `简洁背景`。

检验：手指正常、背景干净、颜色不艳俗、主体突出。

---

## 十九、合规、版权与成本注意点

**合规**：负面提示同样要合规，不能用负面提示去「绕过」限制（如写「非某真人」试图规避）。平台过滤在正负向都生效。

**版权**：负面提示不影响权属，但调优出的成品仍受平台授权约束，商用前核查。

**成本**：系统调优路径把「试错」集中在小图低步数阶段，大幅省算力；固定种子做变体比全随机省。

---

## 二十、本章小结

本章补齐了文生图的两块关键拼图：
- 负面提示划禁区，通用负面 + 专项负面，权重可分级；
- 五参数各管什么，及「先内容后质量、一次一动一变量」的系统调优路径；
- 用实验日志把经验固化。

到本章，你已具备「独立产出干净可控图」的完整能力。下一章（1-6）进入「图生图与局部重绘」，把控制力再提一档。

---

## 二十一、上手练习

1. 用通用负面词库 + 你 1-3 章写的某条提示词生成 2 张，观察是否少了常见瑕疵；再去掉负面生成 2 张对比。
2. 取一条提示，分别设 CFG=8 与 CFG=13 各生成 2 张，描述差异并记录你的偏好值。
3. 按「系统调优路径」完整调一张图，并填写一节的自建记录表（至少 4 轮）。

---

## 二十二、场景参数预设：直接抄的「甜区地图」

把常见场景的参数组合固化，省去每次试错。以下为通用参考（具体依平台微调）：

| 场景 | 步数 | CFG | 比例 | 备注 |
| --- | --- | --- | --- | --- |
| 头像/特写 | 30 | 9 | 方/竖 | 负面重「面部瑕疵」 |
| 封面图 | 32 | 10 | 竖 2:3 | 正向写「留白」 |
| 产品图 | 30 | 8 | 方 | 负面重「脏背景」 |
| 插画 | 28 | 9 | 任意 | 风格词主导 |
| 概念场景 | 35 | 10 | 横 16:9 | 重构图词 |
| 批量变体 | 25 | 9 | 统一 | 固定种子区间 |

用法：先套最近场景的预设，再按结果微调。预设是起点不是终点。

---

## 二十三、负面提示的边界：什么它管不了

负面提示很强，但不是万能。它管不了：

- **精确文字**：想图里出「正确汉字」，负面写「无文字」只能禁掉，不能凭空生成对字。需后期加。
- **精确数量**：写「不多不少 5 只」不保证，负面只能排「多余」，不能保「正好」。
- **特定真人**：这是合规禁区，负面也绕不过平台过滤。
- **逻辑硬约束**：「左边比右边大两倍」这类空间逻辑，模型易错，负面难修。

认知边界，才能把精力放在「负面擅长的事」上，其余交给图生图或后期。

---

## 二十四、参数之间的「联动效应」

参数是互相影响的，单独看会误判：

- **CFG 高 + 步数低**：服从强但没去干净噪，易「硬而糊」。需步数跟上。
- **高分辨率 + 低步数**：图大但糙，细节没时间成形。
- **高 CFG + 强负面**：双重约束，图可能过于「乖」失去灵动，适当降其一。
- **低 CFG + 弱负面**：自由过头，瑕疵泛滥。

所以调优时要把参数当「系统」而非「孤立旋钮」。一次动一个，正是为了看清联动中的主因。

---

## 二十五、常见疑问 FAQ

**Q1：负面提示越长越好吗？**
不是。过长会稀释，甚至干扰正向。通用一版 + 场景专项一版足够。

**Q2：CFG 调到多少最「真实」？**
真实感主要来提示（材质/光线词）与模型，CFG 只管服从度。真实感不在高 CFG。

**Q3：步数 50 一定比 30 好？**
多数情况不会，超过甜区后肉眼难分，纯增成本。

**Q4：为什么固定种子后改提示，图变得陌生？**
种子只固定随机起点，提示大改会指向完全不同区域，正常。小改才见「延续感」。

**Q5：平台已有默认负面，我还要写吗？**
建议补专项负面（你的场景雷区），通用部分可省，避免重复。

---

## 二十六、负面与调优速查卡

- 负面 = 通用默认 + 场景专项，权重核心毛病 1.2。
- 步数 20–40，CFG 7–12，分辨率用推荐。
- 调优顺序：正向定内容 → 小图筛构图 → 步数提细节 → CFG 纠偏 → 负面排瑕 → 固定种子出变体 → 大图定稿。
- 一次只动一个变量，记实验日志。
- 负面管不了：精确文字、精确数量、特定真人、硬逻辑。

把此卡与前面三张卡并列，文生图基础能力已闭环。

---

> 下一章「图生图与局部重绘」：你会学到如何以一张已有图为底做修改、如何只重画画面的某一部分，控制力将再上一个台阶。
